version: "3"

services:
  tile-server:
    restart: always
    container_name: tile-server
    image: maptiler/tileserver-gl
    ports:
      - "80:8080"
    volumes:
      - ${HOME}/data:/data
      - ./styles:/data/styles
      - ./config.json:/data/config.json
